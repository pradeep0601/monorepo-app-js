import { Address } from "cluster";
import { Company } from "./Company";

export interface User {
    id: Number;
    name: String;
    username: String;
    email: String;
    // phone: String;
    // website: String;
    // address: Address;
    // company: Company;
}